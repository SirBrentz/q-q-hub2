2026-05-05  Fix mobile overflow in white-label preview (home + weight-loss)
- On narrow phones the white-label preview bar was pushing past the
  viewport: long brand domains (e.g. medspaaesthetics.com) plus the
  faux "Shop · About · Cart" nav couldn't fit on a single row, and the
  long sender email in the demo email block had no break point.
- Added a max-width:600px media query that trims wl-stage padding to
  22px, hides the faux nav, and tightens preview-bar / preview-body /
  preview-email padding.
- Universally allow the brand-name span to ellipsize and the demo
  sender email to wrap (overflow-wrap:anywhere on the bold email
  address) so longer brands don't blow out the layout.
- Same fix applied to both index.html and weight-loss/index.html.

2026-05-04  Self-host plugin downloads from /downloads/qualiphy.zip
- Added a downloads/ folder at the repo root with a README explaining the
  one-file convention: drop the new build in as qualiphy.zip and overwrite
  on each release. No link changes required between releases.
- Repointed all nine "Download Plugin" buttons (across home, weight-loss,
  get-started, changelog, help, thank-you) from
  github.com/tom-at-Quailiphy/qualiphy-woocommerce/releases/latest/download/qualiphy.zip
  to /downloads/qualiphy.zip so the site serves the file directly instead
  of bouncing users to GitHub.
- Reminder: drop the actual qualiphy.zip into downloads/ before the next
  deploy or every button 404s.

2026-05-01  Hero video card: rename to "Quick Plugin Walkthrough", drop duration
- Renamed the hero floating video card title from "Product walkthrough" to
  "Quick Plugin Walkthrough" on Home and Weight Loss.
- Removed the hard-coded 2:18 duration label since it didn't match the
  actual video length. Removed the now-unused .hv-video-duration CSS rule.

2026-05-01  Hero video card + modal player; thank-you Purchase pixel
- Thank-you page: replaced the Lead pixel with a Purchase event ($500 USD)
  per marketing director's request. Now only fires when invitee_uuid is
  present in the URL (Calendly redirect parameter), de-duped via eventID.
- Home + Weight Loss: added a floating "Product walkthrough" card to the
  hero visual using the new assets/video-welcome.jpg thumbnail with a play
  button overlay. Card animates with the same float keyframe as the other
  hero chips and stacks below the device mockups on mobile.
- Home + Weight Loss: clicking the card opens an in-page lightbox modal
  with a youtube-nocookie.com embed of We4sKXkuvl4 — closes via X button,
  backdrop click, or Escape, and clears the iframe src on close so audio
  stops. Stays on-page so we keep the visitor.
- Home: removed the standalone .how-video iframe (and its CSS) from the
  How It Works section since the hero card now carries the video.

2026-04-30  Changelog: hide unreleased versions
- Changelog page now filters to is_released === true before render. The
  amber "unreleased" treatment and planned-status badges still exist in
  CSS/render code but no longer have anything to attach to. The "On the
  roadmap" (future_planned) section is left as-is — it's a separate
  marketing teaser, not a version.

2026-04-30  Embed marketing video; fix Help Center button readability
- Home: added the Marketing video below the How It Works step cards.
- Help Center: embedded the same video at the top of the Setup & Installation
  articles "System requirements", "Installing the plugin", and "What the
  setup wizard does".
- Help Center: fixed the Download Quidget Plugin button on the Installing
  the plugin article — text was rendering indigo on indigo (unreadable)
  because .hc-art-body a was overriding .btn-primary's white color. Scoped
  override added so .btn variants inside articles keep their proper
  styling. Replaced the trailing "→" with a download icon to match the
  Get Started flow's primary download button.
